__pycache__/
*.pyc
*.pyo
.claude/worktrees/
.claude/settings.local.json
.claude/scheduled_tasks.lock
*.egg-info/
dist/
build/
.env
hosted/secret.json
.mcpregistry_github_token
.mcpregistry_registry_token
.DS_Store
**/.DS_Store
pilot/

# Internal-only files. Kept on local disk; not pushed to the public repo.
# Decision criterion: anything that contains personal data, internal
# operational runbooks, vendor-cost figures, smoke-test instructions, or
# stakeholder-only stakeholder material lives only on the maintainer's
# machine. The product code, public docs, licence, and privacy policy
# remain in git.
CLAUDE.md
BACKLOG.md
TASKS.md
docs/RENDER_CONSOLIDATION.md
docs/TAOS_Architecture_v1.1.docx
profiles/pro-*.md
!profiles/TEMPLATE.md

# Stakeholder distribution docs and the script that generates them.
# Shared by hand, not via GitHub.
docs/TAL_Architecture.docx
docs/generate_architecture_docx.py

# Research / thinking material and working context, kept locally only
# (untracked from the public repo; files remain on the maintainer's disk).
context/
emails/

# Local Claude Code slash commands. The public versions live in plugin/skills/.
.claude/commands/

