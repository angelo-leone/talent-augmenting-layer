# /talent-update

Remote MCP only.

1. Load the current profile with `talent_get_profile`.
2. Check recent progression with `talent_get_progression`.
3. Ask a short update: biggest challenge, new tools, role changes, AI usage changes.
4. Update the profile and save it with `talent_save_profile`.
5. Add a short change-log entry.

Keep it brief and specific.
